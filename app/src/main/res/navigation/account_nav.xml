<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/account_nav"
    app:startDestination="@id/account">

    <fragment
        android:id="@+id/market"
        android:name="com.bagrut.find.MarketList"
        android:label="market"
        tools:layout="@layout/market_list" />
    <fragment
        android:id="@+id/account"
        android:name="com.bagrut.find.AccountActivity"
        android:label="account"
        tools:layout="@layout/activity_account" />
    <fragment
        android:id="@+id/shopping_cart"
        android:name="com.bagrut.find.CartActivity"
        android:label="shopping_cart"
        tools:layout="@layout/activity_cart" />
</navigation>